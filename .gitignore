.DS_Store

/public

# Generated by Zola's classed syntax highlighting (style="class" in config.toml);
# rebuilt into static/ + public/ on every `zola build`, so not source.
/static/giallo-*.css

# Local working source docs (bio data for building About/Projects) — not for the repo
/linkedin-profile.md
/github-profile.md
.playwright-mcp
robin-rendle-breakouts/

# Node / Playwright tooling (the screenshot suite). Committed screenshots live
# in tests/screenshots/ and are intentionally NOT ignored.
node_modules/
test-results/
playwright-report/

# Local-only review page (derived from the screenshots; regenerate with
# `npm run gallery`). The generator tests/gallery.mjs IS committed.
/tests/screenshots/gallery.html
