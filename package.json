{
  "name": "akessonio-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tooling for the akesson.io Zola site (Playwright screenshot suite).",
  "scripts": {
    "screenshots": "zola build --base-url http://127.0.0.1:8787 && playwright test && node tests/gallery.mjs",
    "test": "playwright test",
    "gallery": "node tests/gallery.mjs",
    "check:math": "node scripts/check-no-math.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "pixelmatch": "^7.2.0",
    "pngjs": "^7.0.0"
  }
}
